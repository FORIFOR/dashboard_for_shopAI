# Secrets — never commit
.env
*.env
!.env.example
# backend /metrics token lives here (see backend_sd.json.example)
monitoring/prometheus/backend_sd.json

# Python
__pycache__/
*.pyc
.venv/
venv/

# Local runtime data (Docker named volumes are separate, but ignore stray dumps)
prometheus_data/
grafana_data/
*.log
