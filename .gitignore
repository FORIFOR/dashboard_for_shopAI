# Secrets — never commit
.env
*.env
!.env.example

# Python
__pycache__/
*.pyc
.venv/
venv/

# Local runtime data (Docker named volumes are separate, but ignore stray dumps)
prometheus_data/
grafana_data/
*.log
